Add sound files in this folder using these exact names:

1. `ui-select.mp3`
2. `select-service.mp3`
3. `remove-item.mp3`
4. `notify.mp3`

Where to find royalty-free UI sounds:

1. Mixkit: https://mixkit.co/free-sound-effects/
2. Pixabay Sounds: https://pixabay.com/sound-effects/
3. Freesound: https://freesound.org/ (check each sound license before use)

Recommended search terms:

1. `ui click`
2. `soft pop`
3. `delete click`
4. `notification success`

Notes:

1. Keep clips short (100-600 ms) for UI responsiveness.
2. Normalize/trim audio so volume is consistent.
3. MP3 format is expected by current code paths.
